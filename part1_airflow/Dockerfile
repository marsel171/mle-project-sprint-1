# Кастомное расширение для уже существующего образа c помощью Dockerfile.

# Укажите, какой образ вы расширяете.
FROM apache/airflow:2.7.3-python3.10 

# Скопируйте файл requirements.txt со списком требуемых библиотек 
# с виртуальной машины в изолированное пространство контейнера.
COPY requirements.txt ./tmp/requirements.txt

# Установите все библиотеки в изолированное пространство контейнера. 
# Предварительно на всякий случай обновите pip до последней версии.
RUN pip install -U pip
RUN pip install -r ./tmp/requirements.txt